import type { ScoutOpportunityInput } from "@griffty/domain";

export async function distrokidScout(): Promise<ScoutOpportunityInput[]> {
  // Requires DISTROKID_SESSION_TOKEN in Secret Manager
  return [];
}
